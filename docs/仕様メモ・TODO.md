# 仕様メモ・TODO（今後実装予定）

> 最終更新: 2026-07-15

## 次にやること（優先順）

1. **Slack Webhook接続と本番稼働**（docs/セットアップ手順.md 手順2〜6）— これが済むまでは机上の完成
2. **実配信テスト** — fetch → broadcast を手動実行し、実際にSlackへ届くか確認

## 改善アイデア（優先度低）

- Google News RSSのノイズ・重複対策（別ソースの同一ニュースをタイトル類似で間引く）
- キーワードミュート（「広告」等を含む記事を除外）
- ソースのカテゴリ別に配信を分ける
- Slackスラッシュコマンドでキーワード検索（過去記事をDBから引く）
- 配信件数・時刻の最適化（Slackなら毎時配信も無料で可能）
- Google News RSSのキーワードを増やす（`src/lib/sources.ts` に1行追加でOK）

## 判断待ち・要確認事項

- 【要確認】配信時刻 8/13/19時（JST）でよいか。変えるなら `.github/workflows/cron.yml` のcron式を修正
- 【要確認】1回の配信件数の上限30件でよいか（`MAX_ARTICLES_PER_BROADCAST`）。多い/少ないは調整可能
- 【要確認】Google Newsのノイズが気になる場合、ソースから外すか検討
- 【要確認】gitルートがホームディレクトリになっている件（引き継ぎメモ参照）。プロジェクト単独リポジトリに切り出すか
