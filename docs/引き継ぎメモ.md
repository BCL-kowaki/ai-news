# 引き継ぎメモ

> このファイルだけ読めば、誰でも続きから開発できる状態を保つ。常に最新化する。
> 最終更新: 2026-07-17

## いまの状態

**「AIニュース配信システム」から「AI秘書（秘書エージェント）」への作り替え、フェーズ1〜2（コード）が完了。**
Slack配信は廃止し、Webアプリに一本化した。デザインは当初ベントグリッド（Indigo）で作った後、
ユーザー要望により **iOSネイティブ調**（システムフォント・iOSブルー・すりガラスバー・ウィジェット風カード）へ再刷新した。
ロゴは **SERA**（public/logo.png。ヘッダーはワードマーク、favicon/ホーム画面アイコンも生成済み）。

### フェーズ2でできたこと（コード完成。実アカウント接続はユーザーのGCP作業待ち）
- [x] Google連携の自前OAuthフロー（/api/google/connect → 同意画面 → /api/google/callback）
  - CSRF対策（state + httpOnly Cookie）、ラベル付き複数アカウント対応、同一メールは上書き=再連携
- [x] refresh_token の AES-256-GCM 暗号化保存（src/lib/crypto.ts、鍵は TOKEN_ENCRYPTION_KEY）
- [x] アクセストークンのメモリキャッシュ＋自動更新。invalid_grant で status="expired"（要再連携）に自動マーク
- [x] カレンダー: 全アカウント統合の「今日の予定」（JST・繰り返し展開・重複排除・終日対応・アカウント色分け）
- [x] カレンダー選択ページ（/settings/calendars/[id]）… 家族共用カレンダーはここでチェックして表示に追加
- [x] Gmail: 受信トレイ最新メール（件名・差出人・抜粋・未読。本文は一切保存しない）
- [x] ダッシュボードの予定カード・メールカードを実データ化、/mail ページ新設
- [x] 設定ページ: 連携追加（ラベル指定）・再連携・解除・環境変数の状態表示（フェイルセーフ）

### フェーズ1でできたこと（ローカルで実操作検証済み）
- [x] NextAuth v4によるログイン必須化（Googleサインイン + ALLOWED_EMAIL許可リスト + JWTセッション）
- [x] ミドルウェア・レイアウト・Server Actionの多層防御。cron APIは除外（Bearer認証を実機確認: 未認証401/認証200）
- [x] ベントグリッドダッシュボード（ブリーフィング枠・予定枠・タスク・突発メモ・よく使うメモ・最新ニュース・メール枠）
- [x] タスク管理（追加・完了切替・削除・期限・優先度）
- [x] メモ（突発メモ→よく使うへ昇格 / よく使うメモのコピー・編集・削除）
- [x] ニュースのアプリ内一本化（/news・/news/[ジャンル]、翻訳・要約ボタン継続）
- [x] Slack配信の完全削除（broadcast route / notify / slack / format / cron.ymlのbroadcastジョブ）
- [x] スキーマ拡張（GoogleAccount / Task / Memo / Briefing）→ `prisma db push` 済み
- [x] Next.js 14.2.15 → **14.2.35**（ミドルウェア認証バイパス CVE-2025-29927 対策。ログインを守る要）
- [x] スマホ表示（375px・下部タブバー）確認済み

### 会議機能（録音・文字起こし・レポート・カレンダー登録）を追加（2026-07-17）
- [x] /meetings: ブラウザ録音（MediaRecorder）・音声ファイル取り込み → Vercel Blobへ直接アップロード
- [x] 文字起こし=Gemini API（無料枠）/ レポート要約=Claude Haiku（Markdown・表つき→整形表示）
- [x] 保存時にGoogleカレンダーへ自動登録（`calendar.events` スコープを連携に追加。**既存アカウントは再連携が必要**）
- [ ] ユーザー作業: GEMINI_API_KEY発行・Vercel BlobのStore作成・個人アカウントの再連携

### 未完了（次にやること）
1. **ユーザー作業（フェーズ2を動かすため）**:
   - GCPで **Gmail API** と **Google Calendar API** を有効化
   - OAuthクライアントのリダイレクトURIに `/api/google/callback`（localhost・本番の両方）を追加
   - `.env` に `TOKEN_ENCRYPTION_KEY`（openssl rand -hex 32）を追加
   - ローカルでログイン→設定→「Googleアカウントを連携」で実アカウント接続を確認
2. Vercel環境変数の更新（NEXTAUTH_SECRET / NEXTAUTH_URL / GOOGLE_CLIENT_ID / GOOGLE_CLIENT_SECRET /
   ALLOWED_EMAIL / TOKEN_ENCRYPTION_KEY 追加、SLACK_WEBHOOK_URL 削除）→ 本番デプロイ
3. フェーズ3: AIブリーフィング（/api/cron/briefing + GitHub Actions 朝7時JST）・メール要約/重要度

## ハマりどころ（実際に起きた/起きうる問題と対策）

### 6. Next.js 14.2.15にミドルウェア認証バイパスの重大CVE【今回対応済み】
- `x-middleware-subrequest` ヘッダでミドルウェアを素通りできる脆弱性（CVE-2025-29927）。ログイン必須化が無意味になるところだった。
- **対策**: 14.2.35へ更新済み。`npm audit` に残る警告はNext 15/16でしか直らないもの（DoS系中心）。将来Next 15へ上げる。

### 7. FormData の priority が Number(null)=0 になる罠【今回対応済み】
- ダッシュボードのクイック追加には優先度欄が無く、`Number(formData.get("priority"))` が `Number(null)=0`（低）になっていた。
- **対策**: `formData.get()` の null を先に判定してからデフォルト「中(1)」を適用。

### 8. Server Actionのフォームは送信後に入力が残る
- `<form action={serverAction}>` はNext 14では自動リセットされない。
- **対策**: `src/components/AutoResetForm.tsx`（アクション完了後に form.reset() する客側ラッパー）を全入力フォームで使用。

### 9b. Tailwindで CSS変数色に透明度修飾子は使えない
- 色を `var(--line)` のように定義すると、`divide-line/60` や `ring-accent/50` はビルドエラーまたは無効になる。
- **対策**: 透明度は変数側に焼き込む（`--line: rgba(60,60,67,0.15)`）。修飾子が必要な箇所は `ring-[rgba(0,122,255,0.45)]` のようにリテラルで書く。

### 9. GCP同意画面「テストモード」のrefresh tokenは7日で失効（フェーズ2の前提）
- Gmail readonlyはrestrictedスコープで、個人利用は審査を通さずテストモード運用が現実解。ただしテストモードのトークンは7日で切れる。
- **対策（設計済み）**: 連携カードに状態表示+「再連携」1タップ導線。失効しても他機能は動くフェイルセーフにする。

### 1〜5. 旧システム時代のハマりどころ（有効なものは継続）
- Vercel Hobbyのcronは1日1回まで → 定期実行はGitHub Actions（継続）
- RSSの末尾スラッシュ/リダイレクト、Neonの接続文字列2種、`.next` の型キャッシュ → 従来どおり
- VentureBeat停止・Google Newsノイズ → ソース構成で解決済み（sources.ts）

## 検証用メモ（ローカル）

- ログインにはGCPクライアントが必要。UIだけ確認したいときは `next-auth/jwt` の `encode` でセッショントークンを作り、
  Cookie `next-auth.session-token` に入れる（NEXTAUTH_SECRETは .env のもの）。検証後はCookie削除。
- `.env` にはNextAuth系のキーを追記済み（NEXTAUTH_SECRETは生成済み・Google系は空）。

## リポジトリ構成の注意【重要・継続】

このプロジェクトの **gitリポジトリのルートはホームディレクトリ `/Users/takaya`**（プロジェクト直下ではない）。
`git add -A` をホーム基準で叩くと個人設定や鍵ファイルまで巻き込む危険がある。
コミットは必ず `cd ~/work/myfolda/AIニュース` してから、このフォルダ配下だけを対象にする。

## 検証済みの内容（2026-07-17）

- `npx tsc --noEmit` … エラーなし
- `npx next build` … 成功（12ルート + middleware）
- ブラウザ実操作 … ログインゲート（未ログイン→/login、未設定env案内）/ タスク追加・完了・削除 /
  突発メモ追加・よく使うへ昇格 / ニュース一覧・ジャンルチップ / 設定ページ / スマホ375px表示
- cron認証 … 未認証401 / 認証200（実RSS収集100件成功）
