# 仕様書

> 実装と同期させる。機能を変えたらここも更新する。
> 最終更新: 2026-07-17（秘書エージェント化 フェーズ1）

## 目的

ニュース・予定・メール・タスク・メモを1画面に集約する**個人用の秘書エージェント**Webアプリ。
旧「AIニュース自動配信システム（Slack配信）」を土台に作り替えた。**Slack配信は廃止**し、Webアプリに一本化。

### フェーズ計画

| フェーズ | 内容 | 状態 |
|---|---|---|
| 1 | ログイン必須化・ベントグリッドUI・タスク・メモ・ニュース内蔵 | **完了** |
| 2 | Google連携（複数アカウントのカレンダー・Gmail表示） | 未着手 |
| 3 | AI秘書（朝のブリーフィング自動生成・メール要約/重要度） | 未着手 |

## 技術スタック

- Next.js 14 (App Router) / TypeScript
- Prisma + Neon PostgreSQL
- Tailwind CSS（CSS変数トークン + **iOSネイティブ調デザイン**）
- NextAuth v4（Googleサインイン・JWTセッション・メール許可リスト）
- lucide-react（アイコン）
- ホスティング: Vercel（hnd1） / 定期実行: GitHub Actions（Vercel Hobbyのcron制限回避）
- フォント: Appleシステムフォント（SF Pro / ヒラギノ角ゴ。iPhone/Macでネイティブ表示）

### デザイン（iOSネイティブ調）

- 背景 `#F2F2F7`（iOS systemGroupedBackground）＋枠線・影なしの白カード（ホーム画面ウィジェット風）
- アクセントはiOSブルー `#007AFF`。ボタンは丸形（塗り=白文字 / グレー塗り=ブルー文字）
- 入力欄はグレー塗り・枠線なし（iOS検索フィールド風）。区切りはヘアライン
- ヘッダー・下部タブバーはすりガラス（backdrop-blur）。見出しはラージタイトル（28px bold）
- トークンの単一定義元は `globals.css` の CSS変数（`--bg` `--card` `--accent` `--fill` 等）

## 認証（フェーズ1の中核）

- **全ページログイン必須**。`src/middleware.ts`（next-auth/middleware）が未ログインを `/login` へ送る
- 除外パス: `/api/auth`（NextAuth自身）、`/api/cron`（CRON_SECRETのBearer認証で別途保護）、`/login`、静的ファイル
- Googleサインイン後、`ALLOWED_EMAIL`（カンマ区切り可）に載っているメールだけ許可（`signIn` コールバック）
- フェイルセーフ: 認証系の環境変数が未設定なら誰もログインできず、ログイン画面がセットアップ案内を表示
- `(main)/layout.tsx` でもセッション確認（ミドルウェアとの二重防御）。Server Actionも各アクション先頭で確認
- ※ アプリログインと、フェーズ2のGoogle API連携（GoogleAccountテーブル）は**別物**として分離する設計

## 画面構成

| ルート | 内容 |
|---|---|
| `/login` | Googleサインイン（唯一の認証不要ページ）。環境変数不足時はセットアップ案内 |
| `/` | **ベントグリッドダッシュボード**: ①今日のブリーフィング（フェーズ3まではプレースホルダ） ②今日の予定（フェーズ2まではプレースホルダ） ③タスク（上位5件+クイック追加） ④突発メモ（即入力） ⑤よく使うメモ ⑥最新ニュース6件 ⑦メール（フェーズ2） |
| `/news` | ジャンルチップ（件数つき）+ 最新30件。各記事に「翻訳」「要約」ボタン（オンデマンド） |
| `/news/[category]` | ジャンル別一覧。定義外カテゴリは404 |
| `/tasks` | タスク追加フォーム（期限・優先度）+ 未完了一覧 + 完了済み直近20件 |
| `/memos` | 突発メモ（クイック入力・よく使うへ昇格・削除）/ よく使うメモ（タイトル付き・コピー・編集・削除） |
| `/settings` | アカウント（ログアウト）/ AI・翻訳キーの設定状況 / Google連携一覧（フェーズ2）/ 情報ソース一覧 |

- ナビ: スマホ=下部固定タブバー（セーフエリア対応）、PC=上部ヘッダー。モバイルファースト
- デザイントークンは `globals.css` の CSS変数が単一定義元（Tailwindは名前の割り当てのみ）

## API仕様

### GET/POST `/api/cron/fetch`（認証: Bearer CRON_SECRET）
毎時実行（GitHub Actions）。sources同期 → 各RSS取得 → 48時間以内の新着URLだけ `Article` に保存。
新規記事の英語タイトルはDeepLで日本語訳して `titleJa` に保存。
戻り: `{ ok, inserted, pending, details[] }`

### `/api/auth/[...nextauth]`
NextAuthの全エンドポイント（サインイン・コールバック・セッション）。設定は `src/lib/nextauth.ts`。

### Server Actions
- `src/app/actions.ts` … `translateArticle` / `summarizeArticle`（記事のオンデマンド翻訳・要約）
- `src/app/(main)/tasks/actions.ts` … `createTask` / `toggleTask` / `deleteTask`
- `src/app/(main)/memos/actions.ts` … `createMemo` / `updateMemo` / `promoteMemo` / `deleteMemo`
- すべて先頭でセッション確認（多層防御）

## DBスキーマ（prisma/schema.prisma）

- **Source / Article** … 旧システムから継続（`Article.status` の sent/skipped 更新は廃止。一覧は最新順表示）
- **SendLog** … 旧Slack配信の記録。参照コードは無し（履歴保全のため残置）
- **GoogleAccount** … フェーズ2用。連携Googleアカウント（label/email/暗号化refresh_token/scopes/status/calendarIds/colorHex）
- **Task** … title/note/due/priority(0-2)/status(open・done)/completedAt
- **Memo** … kind("quick"=突発 / "pinned"=よく使う)/title/body/sortOrder
- **Briefing** … dateKey("YYYY-MM-DD" JST・unique)/content。フェーズ3のcronが1日1件生成

## 翻訳（DeepL）と要約（Claude Haiku）

旧システムから継続。どちらも未設定でも壊れないフェイルセーフ。
- タイトル翻訳: 収集時に新規記事のみ（無料枠節約）
- 本文翻訳・要約: 一覧の「翻訳」「要約」ボタンでオンデマンド（要約は `claude-haiku-4-5`・レポート風1000字以内）

## 設定値（すべて `src/lib/config.ts` に集約）

| 定数 | 値 | 意味 |
|---|---|---|
| FETCH_WINDOW_HOURS | 48 | 収集対象の鮮度 |
| RSS_TIMEOUT_MS | 10000 | RSS取得タイムアウト |
| CATEGORIES | 6ジャンル | 名前とチップ配色（bg/fg）の単一定義元 |
| TASK_PRIORITIES | 高/中/低 | 優先度の数値・ラベル・配色 |
| DASHBOARD_* | 5/6/3/4 | ダッシュボード各カードの表示件数 |
| NEWS_LIST_COUNT | 30 | /news の表示件数 |

## 環境変数

| 変数 | 用途 |
|---|---|
| DATABASE_URL / DIRECT_URL | Neon接続（Pooled / Direct） |
| NEXTAUTH_SECRET / NEXTAUTH_URL | セッション暗号化キー / アプリURL |
| GOOGLE_CLIENT_ID / GOOGLE_CLIENT_SECRET | GCPのOAuthクライアント（ログイン用。フェーズ2の連携でも同じものを使う） |
| ALLOWED_EMAIL | ログインを許可するメール（カンマ区切り可） |
| CRON_SECRET | cron認証 |
| DEEPL_API_KEY / ANTHROPIC_API_KEY | 翻訳 / 要約（任意） |
| TOKEN_ENCRYPTION_KEY | フェーズ2で使用（Google連携トークンの暗号化） |
| ~~SLACK_WEBHOOK_URL~~ | **廃止**（削除してよい） |

## セキュリティ

- 全ページログイン必須（ミドルウェア + レイアウト + Server Action の多層防御）
- Cronエンドポイント: `CRON_SECRET` Bearer認証・**未設定なら常に拒否**・定数時間比較
- 秘密情報はすべて環境変数。ログイン画面・設定画面は「未設定のキー名」だけ表示し、値は絶対に出さない
- RSS由来のURLは http/https のみ許可
- Next.jsは14.2.35（ミドルウェア認証バイパス CVE-2025-29927 等のパッチ適用済み）
- 個人用のため noindex

## ファイル構成

```
src/
  middleware.ts                  ログイン必須化（全ページ）
  app/
    layout.tsx                   フォント・bodyのみ
    globals.css                  デザイントークン（CSS変数）+ 共通クラス
    login/                       ログイン画面
    api/auth/[...nextauth]/      NextAuth
    api/cron/fetch/              RSS収集（毎時）
    actions.ts                   記事の翻訳・要約アクション
    (main)/                      ログイン後の画面（共通ヘッダー・タブバー）
      layout.tsx / page.tsx      画面枠 / ダッシュボード
      news/ tasks/ memos/ settings/
  components/                    Nav / TaskItem / ArticleTable / SubmitButton /
                                 AutoResetForm / CopyButton
  lib/
    config.ts      全定数（単一定義元）
    nextauth.ts    ログイン設定（許可リスト・フェイルセーフ）
    sources.ts     RSSソース定義（増やすのはここだけ）
    source-sync.ts / rss.ts / translate.ts / summarize.ts
    datetime.ts    JST日付処理（dateKey・期限ラベル等）
    auth.ts        Cron認証
    prisma.ts
.github/workflows/cron.yml       毎時fetch（broadcastは削除済み）
```

## 今後の拡張（フェーズ2〜3の設計要点）

- Google連携は自前OAuthフロー（`/api/google/connect` → callback）。refresh_tokenはAES-256-GCMで暗号化しGoogleAccountへ
- 家族共用カレンダーは個人アカウントのcalendarList経由で選択（別OAuth不要）
- GCP同意画面はテストモード運用 → **refresh tokenが7日で失効**するため「再連携」ボタンを設ける
- メール本文はDBに保存しない（都度取得+短期キャッシュ）
- ブリーフィング: 朝7時JSTにGitHub Actionsから `/api/cron/briefing` を叩き、Briefingへ保存
